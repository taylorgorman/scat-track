language: node_js
node_js:
  - "stable"
cache:
  directories:
    - node_modules
script:
 - npm run build
install:
  - npm install -g firebase-tools
  - npm install -g react-scripts
deploy:
  provider: firebase
  token:
    secure: $FIREBASE_TOKEN
